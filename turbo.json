{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".svelte-kit/**", "build/**", ".vercel/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
